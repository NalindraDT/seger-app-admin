# Port untuk dev server (Vite) dan runtime Docker (serve)
PORT=3000

# Base URL backend API (wajib HTTPS jika frontend di-deploy via HTTPS)
VITE_API_BASE_URL=https://seger-api.potydev.cloud/api/v1

# Runtime Docker/Dokploy (override tanpa rebuild image)
API_BASE_URL=https://seger-api.potydev.cloud/api/v1
